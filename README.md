# DUPlicate-COPy-paste-rename-things

```
git clone git@github.com:sudodoki/dupcop.git dupcop
cd dupcop
chmod +x dupcop
# add this folder to your $PATH or add ln to /usr/local/bin
npm i
# next line is workaround for inquirer bundled with inquirer-directory having conflicts
rm -rf node_modules/inquirer-directory/node_modules/inquirer
```
